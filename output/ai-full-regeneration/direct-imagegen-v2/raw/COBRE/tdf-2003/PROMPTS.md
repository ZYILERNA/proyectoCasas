# COBRE / tdf-2003 — direct ImageGen provenance

All ten active PNG files were produced as independent built-in ImageGen calls. Every call referenced only `output/ai-full-regeneration/large-doors/COBRE/tdf-2003/base-tight.png`; no accepted output was used as an input and no logo reference was supplied.

Shared prompt constraints: reconstruct the exact frontal TDF-2003 double door; preserve the complete stepped black frame, both leaves, gold inlays and two ornate gold pull handles; render the requested finish; show the complete frame almost edge-to-edge on white; generate no logo, brand, text, letters, numbers, watermark, badge, emblem or pseudotext; no room, floor, people or props.

Finishes generated independently: `original`, `negro`, `wengue`, `gris-oscuro`, `antracita`, `nogal`, `roble`, `gris-claro`, `natural`, `blanco`.

There was no filter, local recolor, mask, crop, resize, composite, format conversion or public write. Four first attempts with excessive white margin are retained under `_rejected/` and are outside the active set.
