# MADERAACÚSTICA/wl-j008 — direct ImageGen prompts

Use case: `precise-object-edit`. Image 1 was always the sole edit target and sole reference: `public/images/PUERTAS/MADERAACÚSTICA/wl-j008/original.webp`.

Shared invariant contract: preserve the complete very tall narrow square-post frame and straight header, plain vertical-grain leaf, and the reference-identical left-side cluster of slim parallel full-height grooves, including exactly the same number, spacing, widths, paths, and distinctive diagonal jog around handle height. Preserve exactly one dark slim horizontal lever on one round rosette and exactly one separate dark round key cylinder below on the left. Front orthographic on pure white. NO_LOGO: no logo, brand, badge, text, symbols, watermark, microtext, or pseudotext. No extra grooves, panels, seams, glass, decorations, hardware, props, wall, floor, perspective, or scene.

Final composition contract: exact 379:1090 canvas proportion and the complete door scaled as large as ImageGen can render it, targeting a 2–8 px-equivalent white hairline on every side and never accepting a measured margin above 12 px. Broad padding and lost geometry are forbidden. Close framing must be created directly by ImageGen; no crop, resize, mask, recolor, filter, composite, conversion, or bitmap processing is allowed.

Independent finish requests: deep neutral matte black (`negro`), very dark espresso-brown wenge (`wengue`), dark charcoal gray (`gris-oscuro`), graphite anthracite (`antracita`), rich warm walnut (`nogal`), warm golden oak (`roble`), light neutral gray wood (`gris-claro`), pale unstained natural wood (`natural`), and clean matte white-painted wood (`blanco`). Each finish was a separate built-in ImageGen call from the canonical `original.webp`.

Retry contract: a fresh call again referenced only the canonical original and repeated every invariant. The single targeted correction was framing: reject broad padding above 12 px; reject uncertifiable multi-edge contact; request a complete near-edge silhouette with a hairline margin. No rejected or accepted generation was ever used as a reference for another call.

Accepted generator IDs by finish: `negro` attempt 4 `exec-71508270-8421-4428-b769-5ef72e9ad2dc`; `wengue` attempt 2 `exec-fd0a4a32-6922-4efa-9e3c-821d48fd7e27`; `gris-oscuro` attempt 4 `exec-2096b704-39aa-4683-bf4a-5d32e48c38ae`; `antracita` attempt 1 `exec-8009b9d4-68b8-43ed-882f-94deee5a93e6`; `nogal` attempt 2 `exec-967b0e90-7cd6-4236-82c2-554db35b7053`; `roble` attempt 2 `exec-958dee6e-4b69-40b6-8684-0ac0557b4fdb`; `gris-claro` attempt 4 `exec-c47e9846-c45c-438e-9c6c-29f052ad5a49`; `natural` attempt 2 `exec-9348a5da-08b0-428e-a124-5da583e539ff`; `blanco` attempt 1 `exec-690440d6-00a2-45b5-869b-d0c1936f6f6f`.

Thirty independent built-in calls were made. Twenty-one rejected PNGs remain byte-for-byte in `_rejected/`, with per-call generator IDs, hashes, measured margins, and rejection reasons in `manifest.json`. The two dark accepted outputs whose bottom margin measures 0 at the RGB<245 threshold were retained only after native-detail inspection confirmed the complete signature and after all alternative calls either contacted another edge or exceeded the absolute 12 px margin ceiling.
